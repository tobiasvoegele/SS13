//Aufgabe 1

a)

funct max (data) returns integer
int maximum = data[0]
  for(int i=1;i<data.length-1;i++) do
    if maximum<data[i] //teure Operation
      maximum = data[i]
    fi
  od
  return maximum
tcnuf

b)
S = O(n)


